query AspectRegistrations($name: [String]) {
    AspectRegistration(enabled: ["true"], name: $name) {
        name
        displayName
        description
        shortName
        unit
        url
        category
        endpoint
        uuid
        enabled
    }
}
