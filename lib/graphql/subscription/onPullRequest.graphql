subscription OnPullRequest {
    PullRequest {
        id
        body
        state
        action
        merged
        head {
            sha
            message
        }
        branchName
        repo {
            owner
            name
        }
    }
}
